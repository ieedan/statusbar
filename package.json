{
  "name": "statusbar",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "description": "StatusBar — a macOS menubar app that watches service status pages.",
  "scripts": {
    "changeset": "changeset",
    "changeset:version": "changeset version"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0"
  }
}
