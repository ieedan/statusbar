{
  "name": "statusbar-adapters",
  "private": true,
  "description": "Built-in StatusBar adapters and the adapter SDK.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p statuspage --noEmit && tsc -p aws --noEmit && tsc -p gcp --noEmit",
    "format": "prettier --write \"**/*.ts\"",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "prettier": "^3.9.4",
    "typescript": "^5.6.0"
  }
}
