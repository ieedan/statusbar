{
  "name": "status-bar-adapters",
  "private": true,
  "description": "Built-in Site Status adapters and the adapter SDK.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p statuspage --noEmit && tsc -p aws --noEmit && tsc -p gcp --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
